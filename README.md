To run the main class in vendor

```bash
java -cp target/rgs-0.1.0-standalone.jar:../vendor/vendor.jar Game
```

To run the main class in platform
```bash
java -cp target/rgs-0.1.0-standalone.jar:../vendor/vendor.jar Platform
```
